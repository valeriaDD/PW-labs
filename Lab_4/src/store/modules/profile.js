
const state = {
    user: {},
    request: {
        profile: false,
    },
};

const getters = {
    getUser(state) {
        return state.user
    }
};

const actions = {};

const mutators = {};

export default {
    state,
    getters,
    actions,
    mutators,
}